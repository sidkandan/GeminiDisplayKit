## What this PR does

(One-line description; reviewers will read this in a list.)

## Why

(Context: bug fix, feature, refactor, doc-only?)

## Pre-merge checklist

- [ ] `npm test` passes
- [ ] `bash scripts/scrub-for-publish.sh` clean (no API keys, no `/Users/` paths)
- [ ] If user-facing, docs in `docs/` updated
- [ ] If new file added, `PROVENANCE.md` updated
- [ ] First-time contributor? Added yourself to `CONTRIBUTORS.md`

## Tested how

(What did you actually run? Don't say "tested" without saying how.)
